<?xml version="1.0"?>
<psalm
    xmlns="https://getpsalm.org/schema/config"
    errorLevel="4"
    resolveFromConfigFile="true"
    findUnusedBaselineEntry="true"
    findUnusedCode="false"
>
    <projectFiles>
        <directory name="assets" />
        <directory name="controllers" />
        <directory name="models" />
        <directory name="migrations" />
        <ignoreFiles>
            <directory name="vendor" />
        </ignoreFiles>
    </projectFiles>

    <stubs>
        <file name="vendor/yiisoft/yii2/Yii.php" />
    </stubs>

    <issueHandlers>
        <!-- HECATE suporta PHP >= 8.1; #[Override] só existe nativamente a partir do PHP 8.3. -->
        <MissingOverrideAttribute errorLevel="suppress" />

        <!-- Yii2 utiliza classes extensíveis por natureza; não forçar finalização automática. -->
        <ClassMustBeFinal errorLevel="suppress" />

        <!-- Propriedades herdadas de yii\db\Migration são inicializadas pelo framework. -->
        <PropertyNotSetInConstructor errorLevel="suppress">
            <directory name="migrations" />
        </PropertyNotSetInConstructor>
    </issueHandlers>
</psalm>
